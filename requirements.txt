# Core dependencies for DyRF-BO and Proximity UQ Benchmarks
numpy>=1.22.0
scipy>=1.8.0
scikit-learn>=1.0.0
matplotlib>=3.5.0
pandas>=1.3.0


# GPU-acceleration (Optional, recommended for Credal UQ on A100)
# Note: For conda environments, it is recommended to install cupy using conda-forge
# to match your specific CUDA toolkit version, e.g.:
# conda install -c conda-forge cupy

# CARP-S Benchmarking Integration
carps
git+https://github.com/automl/HPOBench.git
openml
smac
ioh
ConfigSpace>=0.7.1
hydra-core>=1.3.0
omegaconf>=2.3.0

# Data Analysis, Storage & Statistical Multiplicity Testing
pyarrow>=10.0.0
fastparquet>=2023.1.0
scikit-posthocs>=0.7.0
seaborn>=0.12.0
tabulate>=0.9.0
